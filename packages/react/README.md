# @minnowdb/react

React's concurrent-safe external-store adapter for Minnow live queries, with optional Suspense
and stale-while-revalidate behavior.

See the [React guide](https://minnowdb.com/docs/extensions/react/) for setup and examples.
