// Keep the worker URL relative so every supported bundler can discover and emit this entry.
// The entry itself stays the exact public package subpath applications install.
import "@minnowdb/core/worker";
